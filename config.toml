# Pairs which the bot will be trading with
pairs = ["XBT/USD"]

# Size of the buffer used to calculate the model's parameters
buffer_size = 6000

# Initial model's parameters
a = 0                           # Should be None
k = 0                           # Should be None
sigma = 0                       # Should be None
gamma = 0.05
delta = 1.0

# Trading parameters
qty = 1
max_open_orders = 20
tick_size = 0.5
time_to_sleep = 100